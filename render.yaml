services:
  - type: web
    name: chat
    runtime: python
    region: frankfurt  
    envVars:
      - key: PORT
        value: 10000
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"